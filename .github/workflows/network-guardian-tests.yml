name: Network Guardian tests

on:
  push:
    paths:
      - 'network-guardian/**'
      - '.github/workflows/network-guardian-tests.yml'
  pull_request:
    paths:
      - 'network-guardian/**'
      - '.github/workflows/network-guardian-tests.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run unit tests
        working-directory: network-guardian
        run: python3 -m unittest discover -s tests -v
